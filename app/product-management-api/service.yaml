apiVersion: v1
kind: Service
metadata:
  name: product-management-api
spec:
  type: ClusterIP
  selector:
    app: product-management-api
  ports:
    - name: http
      port: 8080
      targetPort: 8080
