apiVersion: v1
kind: Service
metadata:
  name: product-management-api
spec:
  type: ClusterIP
  selector:
    app: product-management-api
  ports:
    - name: http
      port: 8000
      targetPort: 8000
